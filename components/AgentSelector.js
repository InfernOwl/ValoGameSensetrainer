/* TODO
    - classname css changes
*/

export default function AgentSelector( props ) {
    /* props
        agent - JSON object of the agent
    */

    /*
    for (var [key, value] of Object.entries(agents.Agents)) {
        console.log(`${key}: ${value.moves[0]}`);
        (value.moves).map((item, v) => {
            for (var [ki, val] of Object.entries(item)) {
                console.log(`${ki}: ${val}`);
                console.log(val);
            }
        });
    }
    */

    return (
        <div>
            <div className="movelist">
                <ul>
                    <li>
                        <div>TESTER</div>
                    </li>
                </ul>
            </div>
        </div>
    )
}